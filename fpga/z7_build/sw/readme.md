# sw

Vitis software for the FPGA build.

- Built by `build.bat all` (or `build.bat software`)
- Sources are copied into the Vitis workspace under `fpga/syn/build/vitis_ws`
- Entry point is `video_demo.c`
