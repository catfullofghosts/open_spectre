Python script that used XSDB to program EMS registers, skipping having to debug the FPGA design in C from Vitis


to check:
- regs file is ficked needs to have controlls at hex boundried to amek it easyer to debug!!
  
- analog side has alpha regs??? need to be hooked up to regs file or bypassed
- comparitor is working but span setting doesnt seem to do anything

- 
- analog side seems to be not alive? not seeing any of the analog side going to the digital side
    - osc dont seem active noise doesnt seem active! in ILA? why?
